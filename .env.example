# ========================================
# OuonnkiTV 环境变量配置
# ========================================
# 使用说明：
# 1. 复制此文件为 .env：cp .env.example .env  (Linux/Mac) 或 copy .env.example .env (Windows)
# 2. 根据需要修改 .env 文件中的配置
# 3. 本地开发：pnpm dev
# 4. Docker 首次部署：docker-compose up -d --build
# 5. Docker 仅修改环境变量：docker-compose up -d --force-recreate
# ========================================

# ========================================
# 视频源配置
# ========================================
# 初始视频源配置（可选）
# 支持三种格式：单行JSON、多行JSON、或远程 JSON 文件 URL
# 留空表示不预配置任何视频源，用户可在应用中手动添加

# 方式1：单行 JSON 格式（推荐）
# OKI_INITIAL_VIDEO_SOURCES=[{"id":"source1","name":"示例源1","url":"https://api.example1.com","isEnabled":true}]

# 方式2：远程 JSON 文件 URL
# OKI_INITIAL_VIDEO_SOURCES=https://example.com/video-sources.json

# 留空（默认）
OKI_INITIAL_VIDEO_SOURCES=

# ========================================
# TMDB API 配置
# ========================================
# TMDB API Read Access Token (v4 auth)
# 获取地址: https://www.themoviedb.org/settings/api
OKI_TMDB_API_TOKEN=

# TMDB API 基础地址（可选，留空使用官方默认 https://api.themoviedb.org/3）
# 支持绝对地址和相对路径（示例：/tmdb-api）
OKI_TMDB_API_BASE_URL=

# TMDB 图片基础地址（可选，留空使用官方默认 https://image.tmdb.org/t/p/）
# 支持绝对地址和相对路径（示例：/tmdb-image）
OKI_TMDB_IMAGE_BASE_URL=

# ========================================
# 访问控制（可选）
# ========================================
# 设置后，访问站点需要输入此密码。留空表示公开访问。
OKI_ACCESS_PASSWORD=

# ========================================
# 分析配置（可选）
# ========================================
# 禁用 Vercel Analytics 跟踪（自部署建议设为 true）
OKI_DISABLE_ANALYTICS=true

# ========================================
# 完整配置导入（可选，高级用法）
# ========================================
# 通过一个 JSON 字符串导入完整配置（包含应用设置、视频源和订阅）
# 格式与应用内"导出个人配置"生成的 JSON 一致
# 设置此变量后，上面的 OKI_INITIAL_VIDEO_SOURCES 将被忽略
# OKI_INITIAL_CONFIG='{"settings":{...},"videoSources":[...],"meta":{...}}'

# ========================================
# Docker 构建配置（可选）
# ========================================
# 以下变量用于 Docker 镜像标签和元数据
# 通常在 CI/CD 流程中自动设置
BUILD_DATE=
VCS_REF=
VERSION=dev
